{{- if .Values.enableNetworkPolicy }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: thmanyah-store
spec:
  podSelector: {}
  policyTypes: ["Ingress"]
{{- end }}
